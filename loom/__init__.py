"""loom — the engine that makes flow propose its own missions, weigh what
surprised it, and reassemble the gallery from that data.

flow is a collection of emergent systems. loom is the move that turns the
collection itself into one: the same loop the human runs by hand — propose a
"simple rules → emergent behaviour" mission, build it, keep what surprised you —
made explicit and partly self-running. The gallery is the murmuration that
falls out.
"""

__all__ = ["registry", "primitives", "propose", "surprise", "gallery"]
